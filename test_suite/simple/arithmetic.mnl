fun sumGaussN: [n] => {
    return n * (n + 1) / 2
}

fun averageTwo: [a, b] => {
    return (a + b) / 2
}

fun main: [args] => {
    assertTrue(sumGaussN(10) == 55)
    assertTrue(averageTwo(10, 20) == 15)
}
