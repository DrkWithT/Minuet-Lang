# naive recursive fibonacci #

fun fibo: [n] => {
    if n < 2 {
        return n
    } else {
        return fibo(n - 1) + fibo(n - 2)
    }
}

fun main: [args] => {
    def ans = fibo(30)

    print(ans)

    assertTrue(ans >= 832040)
}
